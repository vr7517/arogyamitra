export const apiRoutes = {
  auth: '/api/auth',
  medicines: '/api/medicines',
  pharmacies: '/api/pharmacies',
  inventory: '/api/inventory',
  reservations: '/api/reservations',
}

export async function futureApiClient() {
  throw new Error('Backend integration placeholder. Replace dummy data calls with API requests here.')
}
